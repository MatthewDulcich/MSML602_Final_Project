#preprocessing and pipeline and load pickled file

# Test function
def predict_pipeline(sample):
    return {'text': 'test text', 'pred': int(0), 'label': 'cat?'}


if __name__ == "__main__":
     print(predict_pipeline("hi"))